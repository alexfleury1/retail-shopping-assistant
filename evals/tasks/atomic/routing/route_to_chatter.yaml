# Routing test - verify planner routes general chat to chatter agent

task_id: "route_chat_001"
name: "Route greeting to chatter agent"
type: "atomic"
category: "routing"

turns:
  - user: "Hello!"

init:
  cart: []
  context: ""

verify:
  routing:
    expected: "chatter"
  response:
    not_empty: true

failure_modes:
  - "wrong_routing"

tags:
  - "routing"
  - "chat"
